'use client';import{useEffect,useState}from'react';import{useParams}from'next/navigation';import Link from'next/link';import{getOccurrence,closureCheck,closeOccurrence,Occurrence}from'../../../../lib/store';export default function Page(){const{id}=useParams<{id:string}>();const[o,setO]=useState<Occurrence|null>();const[p,setP]=useState<string[]>([]);useEffect(()=>{setO(getOccurrence(id)||null);setP(closureCheck(id))},[id]);if(!o)return <main className="standalone">Carregando...</main>;function submit(e:any){e.preventDefault();const d:any=Object.fromEntries(new FormData(e.currentTarget).entries());const x=closeOccurrence({...d,occurrenceId:id,closedAt:new Date().toISOString()});if(x.length){setP(x);return}window.location.href='/ocorrencias/'+id}return <main className="standalone"><div className="topline"><div><p>SSMA • ENCERRAMENTO</p><h1>Encerrar ocorrência</h1><div className="crumb">{o.id}</div></div><Link className="btnlink" href={'/ocorrencias/'+id}>Voltar</Link></div>{p.length>0?<section className="panel closureblock"><h2>ENCERRAMENTO BLOQUEADO</h2><p>Existem requisitos pendentes antes do fechamento:</p>{p.map(x=><div key={x}>• {x}</div>)}</section>:<section className="panel closureok"><h2>Requisitos atendidos</h2><p>A ocorrência está apta para encerramento formal pelo SSMA.</p></section>}<form onSubmit={submit}><section className="panel formsection"><h2>Conclusão final</h2><label>Conclusão<textarea name="conclusion" rows={4} required/></label><label className="wide">Lições aprendidas<textarea name="lessons" rows={3}/></label><label className="wide">Recomendações<textarea name="recommendations" rows={3}/></label><label className="wide">Responsável pelo encerramento<input name="responsible" required/></label></section><div className="formactions"><button type="submit" disabled={p.length>0}>Encerrar ocorrência</button></div></form></main>}