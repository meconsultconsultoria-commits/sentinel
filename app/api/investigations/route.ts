import { getCloudflareContext } from "@opennextjs/cloudflare";
export const runtime="nodejs"; export const dynamic="force-dynamic";
type Env={DB:any};
export async function GET(req:Request){const{env}=getCloudflareContext() as unknown as {env:Env};const id=new URL(req.url).searchParams.get("occurrenceId");if(!id)return Response.json(null);const r=await env.DB.prepare("SELECT * FROM investigations WHERE occurrence_id=?").bind(id).first();if(!r)return Response.json(null);return Response.json({occurrenceId:r.occurrence_id,validatedBird:r.validated_bird,reclassReason:r.reclass_reason||"",immediateCause:r.immediate_cause||"",contributingFactors:JSON.parse(r.contributing_factors||"[]"),rootCause:r.root_cause||"",method:r.method||"",why1:r.why1||"",why2:r.why2||"",why3:r.why3||"",why4:r.why4||"",why5:r.why5||"",conclusion:r.conclusion||"",updatedAt:r.updated_at})}
export async function POST(req:Request){const{env}=getCloudflareContext() as unknown as {env:Env};const i:any=await req.json();const now=new Date().toISOString();await env.DB.prepare(`INSERT INTO investigations(id,occurrence_id,validated_bird,reclass_reason,immediate_cause,contributing_factors,root_cause,method,why1,why2,why3,why4,why5,conclusion,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?) ON CONFLICT(occurrence_id) DO UPDATE SET validated_bird=excluded.validated_bird,reclass_reason=excluded.reclass_reason,immediate_cause=excluded.immediate_cause,contributing_factors=excluded.contributing_factors,root_cause=excluded.root_cause,method=excluded.method,why1=excluded.why1,why2=excluded.why2,why3=excluded.why3,why4=excluded.why4,why5=excluded.why5,conclusion=excluded.conclusion,updated_at=excluded.updated_at`).bind("INV-"+i.occurrenceId,i.occurrenceId,i.validatedBird,i.reclassReason||null,i.immediateCause||null,JSON.stringify(i.contributingFactors||[]),i.rootCause||null,i.method||null,i.why1||null,i.why2||null,i.why3||null,i.why4||null,i.why5||null,i.conclusion||null,now).run();await env.DB.prepare("UPDATE occurrences SET bird_validated=?,status='Em investigação',updated_at=? WHERE id=?").bind(i.validatedBird,now,i.occurrenceId).run();await env.DB.prepare("INSERT INTO audit_log(occurrence_id,event,details) VALUES(?,?,?)").bind(i.occurrenceId,"Investigação atualizada","Classificação Bird validada e investigação salva").run();return Response.json({ok:true})}
