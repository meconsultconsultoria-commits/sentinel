import{audit,clearSessionCookie,destroySession,getCurrentUser,getDB}from'../../../../lib/auth-server';export const runtime='nodejs';export const dynamic='force-dynamic';export async function POST(req:Request){const user=await getCurrentUser(req);if(user)await audit(getDB(),user.id,'Logout realizado','Sessão encerrada pelo usuário');await destroySession(req);return new Response(JSON.stringify({ok:true}),{headers:{'content-type':'application/json','set-cookie':clearSessionCookie()}})}